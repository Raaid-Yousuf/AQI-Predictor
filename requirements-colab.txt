# Colab already has tensorflow, numpy, pandas, scikit-learn, matplotlib preinstalled —
# do NOT reinstall those here, it drags in a newer protobuf that breaks Colab's
# own preinstalled Google packages. Only install what's actually missing.
mlflow>=2.12.0,<2.20.0
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
shap>=0.45.0
# Deliberately NOT pinning protobuf — let it stay whatever Colab already has.
# Pinning it either direction just flips which preinstalled package complains.
# The complaints are non-fatal pip resolver warnings, not real failures.